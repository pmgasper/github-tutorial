Sample commands:
  git add
  git commit -m "Comment"
  git push orgin master

  git status
  git diff

  git checkout -b add_excitement
  git checkout master
  git merge add_excitement

  git branch -d add_excitement
  git push origin --delete add_excitement


Files:
  .gitignore # files named will not be tracked

  
